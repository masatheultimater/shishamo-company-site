# Testing Rules
- TDD when feasible: write test → implement → refactor
- AAA pattern: Arrange → Act → Assert
- Target 80% code coverage for new code
- Run related tests after every change
- On test failure: analyze root cause before fixing
- Integration tests for API endpoints and DB operations
