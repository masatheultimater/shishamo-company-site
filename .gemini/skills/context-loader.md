# Context Loader for Gemini CLI

Before starting any task, load the shared context:
1. Read `.claude/rules/` for coding standards
2. Read `.claude/docs/DESIGN.md` for design decisions
3. Read relevant `.claude/docs/research/` files
4. Check `.claude/docs/libraries/` for dependency constraints
