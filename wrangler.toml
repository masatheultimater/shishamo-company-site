# ========================================
# Cloudflare Wrangler設定
# ========================================

name = "shishamo-company-site"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# 環境変数
# PUBLIC_* : .env.production に定義（ビルド時にHTMLへ埋め込まれる公開値）
# シークレット（MICROCMS_API_KEY等）: ダッシュボードの Secrets で管理
#   Settings > Variables and Secrets > Add > Encrypt

# ルーティング設定
# [[routes]]
# pattern = "shishamo-company.com/*"
# custom_domain = true
