---
import Layout from '../layouts/Layout.astro';
import { images } from '../lib/siteConfig';
---

<Layout
  title="ししゃも｜吉川昌宏 - IT × 経営 × 財務"
  description="中小企業の伴走パートナー「ししゃも｜吉川昌宏」。DX推進・生成AI導入・データ活用・経営支援で、御社の挑戦を後押しします。"
  currentPage="home"
>
  <!-- HERO -->
  <section class="hero hero--bg-full">
    <div class="hero-inner">
      <div class="hero-content">
        <div class="hero-badge">中小企業診断士(登録予定) × ITストラテジスト</div>
        <h1 class="hero-title">
          IT・データ・財務の力で<br>
          <span class="highlight">経営の課題</span>を解決します
        </h1>
        <p class="hero-desc">
          Web開発12年、会計事務所1年の経験を活かし、DX推進から経営改善まで。
          中小企業の「なんとかしたい」に寄り添い、伴走支援いたします。
        </p>
        <div class="hero-cta">
          <a href="/contact/" class="btn-primary">無料相談を予約する <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg></a>
          <a href="/services/" class="btn-secondary">サービス一覧を見る</a>
        </div>
      </div>
      <div class="profile-card">
        <div class="profile-photo">
          <img src={images.profileImage} alt="吉川昌宏" />
        </div>
        <div class="profile-nickname">ししゃも</div>
        <div class="profile-name">吉川 昌宏</div>
        <div class="profile-title">中小企業診断士(登録予定) / ITストラテジスト</div>
        <div class="profile-quals">
          <span class="qual-badge highlight">中小企業診断士(登録予定)</span>
          <span class="qual-badge highlight">ITストラテジスト</span>
          <span class="qual-badge">E資格</span>
          <span class="qual-badge">DB スペシャリスト</span>
        </div>
        <div class="profile-stats">
          <div class="stat-item">
            <div class="stat-value">12<span class="stat-unit">年</span></div>
            <div class="stat-label">システム開発</div>
            <div class="stat-tooltip">
              <ul>
                <li>Webアプリケーション開発</li>
                <li>業務システム設計・構築</li>
                <li>データベース設計・運用</li>
                <li>API開発・連携</li>
              </ul>
            </div>
          </div>
          <div class="stat-item">
            <div class="stat-value">1<span class="stat-unit">年</span></div>
            <div class="stat-label">会計事務所</div>
            <div class="stat-tooltip">
              <ul>
                <li>記帳代行・月次決算</li>
                <li>法人・個人の税務申告</li>
                <li>経営分析・財務アドバイス</li>
              </ul>
            </div>
          </div>
          <div class="stat-item">
            <div class="stat-value">5<span class="stat-unit">+</span></div>
            <div class="stat-label">国家資格</div>
            <div class="stat-tooltip">
              <ul>
                <li>中小企業診断士(登録予定)</li>
                <li>ITストラテジスト</li>
                <li>データベーススペシャリスト</li>
                <li>応用情報技術者</li>
                <li>E資格（JDLA認定）</li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- QUICK ACCESS -->
  <section class="quick-access">
    <div class="quick-cards">
      <a href="/services/" class="quick-card">
        <span class="quick-icon">🚀</span>
        <span class="quick-label">サービス</span>
        <span class="quick-desc">何ができる？</span>
      </a>
      <a href="/profile/" class="quick-card">
        <span class="quick-icon">👤</span>
        <span class="quick-label">プロフィール</span>
        <span class="quick-desc">どんな人？</span>
      </a>
      <a href="/contact/" class="quick-card">
        <span class="quick-icon">💬</span>
        <span class="quick-label">お問い合わせ</span>
        <span class="quick-desc">相談したい</span>
      </a>
    </div>
  </section>

  <!-- PROBLEMS -->
  <section class="section problems">
    <div class="container">
      <div class="section-header">
        <div class="section-label">Problems</div>
        <h2 class="section-title">こんなお悩みはありませんか？</h2>
        <p class="section-desc">一つでも当てはまれば、お力になれるかもしれません</p>
      </div>
      <div class="problem-grid">
        <a href="/services/#dx" class="problem-card">
          <span class="problem-text">DXって何から始めればいいかわからない</span>
          <svg class="problem-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
        <a href="/services/#ai" class="problem-card">
          <span class="problem-text">ChatGPTを業務に活用したい</span>
          <svg class="problem-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
        <a href="/services/#data" class="problem-card">
          <span class="problem-text">データはあるけど活用できていない</span>
          <svg class="problem-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
        <a href="/services/#accounting" class="problem-card">
          <span class="problem-text">経理業務の負担を減らしたい</span>
          <svg class="problem-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
        <a href="/services/#management" class="problem-card">
          <span class="problem-text">経営を客観的に分析してほしい</span>
          <svg class="problem-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
        <a href="/services/#subsidy" class="problem-card">
          <span class="problem-text">補助金を使いたいが申請が難しい</span>
          <svg class="problem-arrow" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg>
        </a>
      </div>
      <div class="problem-cta">
        <p>どれに当てはまるかわからなくても大丈夫です。まずはお気軽にご相談ください。</p>
        <a href="/contact/" class="btn-primary">無料相談を予約する <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg></a>
      </div>
    </div>
  </section>

  <!-- SERVICES -->
  <section class="section services">
    <div class="container">
      <div class="section-header">
        <div class="section-label">Services</div>
        <h2 class="section-title">3つの専門分野でサポート</h2>
        <p class="section-desc">IT・データ・経営財務を横断的に支援できるのが強みです</p>
      </div>
      <div class="service-grid">
        <div class="service-card">
          <div class="service-number">01</div>
          <h3>DX推進・IT戦略</h3>
          <p class="service-desc">デジタル技術を活用した業務改善・競争力強化を支援します。</p>
          <ul>
            <li>DX推進サポート</li>
            <li>IT戦略策定支援</li>
            <li>生成AI導入支援</li>
            <li>Webサイト・アプリ開発</li>
          </ul>
          <a href="/services/#dx" class="service-link">詳しく見る <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg></a>
        </div>
        <div class="service-card">
          <div class="service-number">02</div>
          <h3>データ活用</h3>
          <p class="service-desc">経営データを分析し、意思決定を支援する仕組みを構築します。</p>
          <ul>
            <li>経営データ分析</li>
            <li>KPIダッシュボード構築</li>
            <li>業務プロセスの可視化</li>
          </ul>
          <a href="/services/#data" class="service-link">詳しく見る <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg></a>
        </div>
        <div class="service-card">
          <div class="service-number">03</div>
          <h3>経営・財務支援</h3>
          <p class="service-desc">経営診断から資金調達まで、経営基盤の強化を支援します。</p>
          <ul>
            <li>経営診断・改善提案</li>
            <li>補助金申請サポート</li>
            <li>記帳代行・経理支援</li>
          </ul>
          <a href="/services/#management" class="service-link">詳しく見る <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg></a>
        </div>
      </div>
      <div class="services-all">
        <a href="/services/" class="btn-secondary">すべてのサービスを見る <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg></a>
      </div>
    </div>
  </section>

  <!-- WHY ME -->
  <section class="why-me">
    <div class="why-me-container">
      <div class="section-header">
        <div class="section-label section-label--light">Why Choose Me</div>
        <h2 class="section-title section-title--light">選ばれる3つの理由</h2>
        <p class="section-desc section-desc--light">専門性と実務経験の両方を活かし、実行可能な支援を提供します</p>
      </div>
      <div class="why-grid">
        <div class="why-card">
          <div class="why-number">01</div>
          <h3>IT×経営×財務の複合スキル</h3>
          <p>システム開発から経営診断、財務分析まで一貫して対応。部分最適ではなく、全体最適な提案ができます。</p>
        </div>
        <div class="why-card">
          <div class="why-number">02</div>
          <h3>伴走型の支援スタイル</h3>
          <p>提案だけでなく、実装・運用まで一緒に取り組みます。「絵に描いた餅」で終わらせません。</p>
        </div>
        <div class="why-card">
          <div class="why-number">03</div>
          <h3>わかりやすいコミュニケーション</h3>
          <p>専門用語を使わず、経営者目線でご説明します。「何を聞いていいかわからない」という方も安心してご相談ください。</p>
        </div>
      </div>
    </div>
  </section>

  <!-- BLOG -->
  <section class="section blog" id="blog">
    <div class="container">
      <div class="section-header">
        <div class="section-label">Blog</div>
        <h2 class="section-title">ブログ・お役立ち情報</h2>
      </div>
      <div class="blog-placeholder">
        <p class="blog-status">Coming Soon</p>
        <h3>ブログ準備中</h3>
        <p>DX推進・AI活用・経営改善のノウハウを発信予定です。</p>
        <div class="external-links">
          <a href="https://x.com/shishamoex09" target="_blank" rel="noopener noreferrer" class="external-link" aria-label="X @shishamoex09（外部サイト、新しいタブで開きます）">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
            @shishamoex09
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6M15 3h6v6M10 14L21 3"/></svg>
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="section cta">
    <div class="cta-inner">
      <h2 class="cta-title">まずはお気軽にご相談ください</h2>
      <p class="cta-desc">
        「こんなこと相談していいのかな？」という内容でも大歓迎です。<br>
        初回30分の無料相談で、御社の課題を一緒に整理しましょう。
      </p>
      <a href="/contact/" class="btn-primary">無料相談を予約する <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M12 5l7 7-7 7"/></svg></a>
      <p class="cta-note">※ 相談だけでも問題ありません。押し売りは一切いたしません。</p>
    </div>
  </section>
</Layout>

<style>
  /* ===== LAYOUT ===== */
  .container {
    max-width: var(--container-lg);
    margin: 0 auto;
    padding: 0 24px;
  }

  /* ===== HERO ===== */
  .hero {
    padding: 120px 24px 80px;
    background: var(--gray-50);
    position: relative;
    overflow: hidden;
  }
  .hero--bg-full {
    background: var(--gray-50) url('/wp1.png') center center / cover no-repeat;
  }
  .hero--bg-full::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(248, 250, 252, 0.85) 0%, rgba(248, 250, 252, 0.6) 100%);
  }
  .hero--bg-full .hero-inner {
    position: relative;
    z-index: 1;
  }
  :global([data-theme="dark"]) .hero--bg-full::before {
    background: linear-gradient(135deg, rgba(15, 23, 42, 0.9) 0%, rgba(15, 23, 42, 0.65) 100%);
  }
  @media (prefers-color-scheme: dark) {
    :global(html:not([data-theme="light"])) .hero--bg-full::before {
      background: linear-gradient(135deg, rgba(15, 23, 42, 0.9) 0%, rgba(15, 23, 42, 0.65) 100%);
    }
  }
  .hero-inner {
    max-width: var(--container-lg);
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 400px;
    gap: 64px;
    align-items: center;
  }
  .hero-content {
    max-width: 560px;
  }
  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--gray-200);
    color: var(--gray-600);
    padding: 6px 14px;
    border-radius: var(--radius-sm);
    font-size: 12px;
    font-weight: 500;
    margin-bottom: 20px;
  }
  .hero-title {
    font-family: var(--font-display);
    font-size: 36px;
    font-weight: 700;
    line-height: 1.4;
    color: var(--dark);
    margin-bottom: 16px;
  }
  .hero-title .highlight {
    color: var(--primary);
  }
  .hero-desc {
    font-size: 15px;
    color: var(--gray-600);
    line-height: 1.8;
    margin-bottom: 28px;
  }
  .hero-cta {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }
  .btn-primary {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: var(--primary);
    color: var(--btn-text-on-primary);
    padding: 12px 24px;
    border-radius: var(--radius-sm);
    font-weight: 600;
    font-size: 14px;
    transition: all 0.2s;
    border: none;
    cursor: pointer;
  }
  .btn-primary svg {
    transition: transform 0.2s;
  }
  .btn-primary:hover {
    background: var(--primary-light);
  }
  .btn-primary:hover svg {
    transform: translateX(4px);
  }
  :global([data-theme="dark"]) .btn-primary {
    color: var(--btn-text-on-primary-dark);
  }
  @media (prefers-color-scheme: dark) {
    :global(html:not([data-theme="light"])) .btn-primary {
      color: var(--btn-text-on-primary-dark);
    }
  }
  .btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: transparent;
    color: var(--dark);
    padding: 12px 24px;
    border-radius: var(--radius-sm);
    font-weight: 600;
    font-size: 14px;
    border: 1px solid var(--gray-300);
    transition: all 0.2s;
    cursor: pointer;
  }
  .btn-secondary svg {
    transition: transform 0.2s;
  }
  .btn-secondary:hover {
    border-color: var(--dark);
  }
  .btn-secondary:hover svg {
    transform: translateX(4px);
  }

  /* Profile Card */
  .profile-card {
    background: var(--gray-50);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    padding: 32px 28px;
    border: 1px solid var(--gray-200);
    position: relative;
  }
  .profile-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 20px;
    right: 20px;
    height: 3px;
    background: var(--primary);
    border-radius: 0 0 2px 2px;
  }
  .profile-photo {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    margin: 0 auto 12px;
    overflow: hidden;
    border: 3px solid var(--primary);
  }
  .profile-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .profile-nickname {
    font-size: 12px;
    font-weight: 600;
    color: var(--primary);
    text-align: center;
    margin-bottom: 4px;
    letter-spacing: 0.05em;
  }
  .profile-name {
    font-family: var(--font-display);
    font-size: 18px;
    font-weight: 700;
    text-align: center;
    margin-bottom: 4px;
    color: var(--dark);
  }
  .profile-title {
    font-size: 12px;
    color: var(--gray-500);
    text-align: center;
    margin-bottom: 20px;
  }
  .profile-quals {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    justify-content: center;
    margin-bottom: 20px;
  }
  .qual-badge {
    font-size: 11px;
    font-weight: 500;
    background: var(--gray-200);
    color: var(--gray-600);
    padding: 5px 10px;
    border-radius: 100px;
  }
  .qual-badge.highlight {
    background: var(--gray-300);
    color: var(--gray-700);
  }
  .profile-stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
    padding-top: 20px;
    border-top: 1px solid var(--gray-200);
  }
  .stat-item {
    text-align: center;
    position: relative;
    cursor: pointer;
  }
  .stat-value {
    font-family: var(--font-sans);
    font-size: 28px;
    font-weight: 700;
    color: var(--primary);
  }
  .stat-unit {
    font-size: 14px;
    font-weight: 500;
  }
  .stat-label {
    font-size: 11px;
    color: var(--gray-500);
    margin-top: 2px;
  }
  /* Tooltip */
  .stat-tooltip {
    position: absolute;
    bottom: calc(100% + 12px);
    left: 50%;
    transform: translateX(-50%);
    background: var(--dark);
    color: var(--gray-50);
    padding: 12px 16px;
    border-radius: var(--radius-sm);
    font-size: 12px;
    line-height: 1.6;
    white-space: nowrap;
    opacity: 0;
    visibility: hidden;
    transition: all 0.2s;
    z-index: 10;
    box-shadow: var(--shadow-lg);
  }
  .stat-tooltip::after {
    content: '';
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    border: 6px solid transparent;
    border-top-color: var(--dark);
  }
  .stat-item:hover .stat-tooltip {
    opacity: 1;
    visibility: visible;
  }
  .stat-tooltip ul {
    text-align: left;
    margin: 0;
    padding: 0;
    list-style: none;
  }
  .stat-tooltip li {
    padding: 2px 0;
  }
  :global([data-theme="dark"]) .stat-tooltip {
    background: var(--gray-100);
    color: var(--dark);
  }
  :global([data-theme="dark"]) .stat-tooltip::after {
    border-top-color: var(--gray-100);
  }
  @media (prefers-color-scheme: dark) {
    :global(html:not([data-theme="light"])) .stat-tooltip {
      background: var(--gray-100);
      color: var(--dark);
    }
    :global(html:not([data-theme="light"])) .stat-tooltip::after {
      border-top-color: var(--gray-100);
    }
  }

  /* ===== SECTION COMMON ===== */
  .section {
    padding: 80px 24px;
  }
  .section-header {
    text-align: center;
    margin-bottom: 48px;
  }
  .section-label {
    font-size: 11px;
    font-weight: 600;
    color: var(--gray-400);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin-bottom: 10px;
  }
  .section-title {
    font-family: var(--font-display);
    font-size: 26px;
    font-weight: 700;
    color: var(--dark);
    margin-bottom: 12px;
  }
  .section-desc {
    font-size: 15px;
    color: var(--gray-600);
    max-width: 520px;
    margin: 0 auto;
    line-height: 1.7;
  }

  /* ===== QUICK ACCESS ===== */
  .quick-access {
    padding: 2.5rem 2rem;
    background: var(--white);
    border-bottom: 1px solid var(--gray-200);
    position: relative;
  }
  .quick-access::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--primary), var(--accent), var(--secondary));
  }
  .quick-cards {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    flex-wrap: wrap;
    max-width: 900px;
    margin: 0 auto;
  }
  .quick-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.625rem;
    padding: 1.5rem 2.5rem;
    background: linear-gradient(135deg, var(--gray-100), var(--gray-50));
    border-radius: var(--radius-md);
    transition: all 0.3s;
    min-width: 180px;
    border: 2px solid transparent;
    position: relative;
    overflow: hidden;
  }
  .quick-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    opacity: 0;
    transition: opacity 0.3s;
  }
  .quick-card:hover::before {
    opacity: 1;
  }
  .quick-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg), 0 0 30px rgba(37,99,235,0.2);
  }
  .quick-card > * {
    position: relative;
    z-index: 1;
    transition: color 0.3s;
  }
  .quick-card:hover > * {
    color: #ffffff;
  }
  .quick-icon {
    font-size: 2rem;
  }
  .quick-label {
    font-weight: 700;
    font-size: 1rem;
  }
  .quick-desc {
    font-size: 0.8rem;
    opacity: 0.8;
  }

  /* ===== PROBLEMS ===== */
  .problems {
    background: var(--gray-50);
  }
  .problem-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    max-width: 960px;
    margin: 0 auto 40px;
  }
  .problem-card {
    background: var(--gray-100);
    border: 1px solid var(--gray-200);
    border-radius: var(--radius-sm);
    padding: 18px 20px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    transition: all 0.2s;
  }
  .problem-card:hover {
    border-color: var(--primary);
    background: var(--gray-50);
  }
  .problem-card:hover .problem-arrow {
    color: var(--primary);
    transform: translateX(4px);
  }
  .problem-text {
    font-size: 14px;
    color: var(--gray-700);
    font-weight: 500;
    line-height: 1.5;
  }
  .problem-arrow {
    color: var(--gray-400);
    flex-shrink: 0;
    transition: all 0.2s;
  }
  .problem-cta {
    text-align: center;
  }
  .problem-cta p {
    color: var(--gray-500);
    font-size: 15px;
    margin-bottom: 20px;
  }

  /* ===== SERVICES ===== */
  .services {
    background: var(--gray-100);
  }
  .service-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
    max-width: 1000px;
    margin: 0 auto;
  }
  .service-card {
    background: var(--gray-50);
    border: 1px solid var(--gray-200);
    border-radius: var(--radius-sm);
    padding: 28px 24px;
    transition: all 0.2s;
  }
  .service-card:hover {
    border-color: var(--primary);
  }
  .service-number {
    font-family: var(--font-sans);
    font-size: 12px;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 16px;
    letter-spacing: 0.05em;
  }
  .service-card h3 {
    font-family: var(--font-display);
    font-size: 17px;
    font-weight: 700;
    margin-bottom: 8px;
    color: var(--dark);
  }
  .service-desc {
    font-size: 13px;
    color: var(--gray-500);
    margin-bottom: 16px;
    line-height: 1.6;
  }
  .service-card ul {
    margin-bottom: 20px;
  }
  .service-card li {
    font-size: 13px;
    color: var(--gray-600);
    padding: 5px 0;
    padding-left: 16px;
    position: relative;
  }
  .service-card li::before {
    content: '\B7';
    position: absolute;
    left: 4px;
    top: 5px;
    font-size: 14px;
    color: var(--gray-400);
    font-weight: 700;
  }
  .service-link {
    font-size: 13px;
    font-weight: 500;
    color: var(--primary);
    display: inline-flex;
    align-items: center;
    gap: 6px;
  }
  .service-link svg {
    transition: transform 0.2s;
  }
  .service-link:hover svg {
    transform: translateX(4px);
  }
  .services-all {
    text-align: center;
    margin-top: 40px;
  }

  /* ===== WHY ME ===== */
  .why-me {
    background: #1a202c url('/wp2.png') center center / cover no-repeat;
    color: #e2e8f0;
    position: relative;
    overflow: hidden;
    padding: 80px 24px;
  }
  .why-me::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, #63b3ed, #4fd1c5);
    z-index: 2;
  }
  .why-me::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(26, 32, 44, 0.92) 0%, rgba(26, 32, 44, 0.85) 100%);
    z-index: 0;
  }
  .why-me .section-label {
    color: rgba(255, 255, 255, 0.7);
  }
  .why-me .section-title {
    color: #e2e8f0;
  }
  .why-me .section-desc {
    color: rgba(255, 255, 255, 0.75);
  }
  :global([data-theme="dark"]) .why-me {
    background: var(--gray-200) url('/wp2.png') center center / cover no-repeat;
    color: var(--dark);
  }
  :global([data-theme="dark"]) .why-me::before {
    background: linear-gradient(90deg, var(--primary), var(--teal));
  }
  :global([data-theme="dark"]) .why-me::after {
    background: linear-gradient(135deg, rgba(51, 65, 85, 0.92) 0%, rgba(51, 65, 85, 0.85) 100%);
  }
  :global([data-theme="dark"]) .why-me .section-label {
    color: var(--gray-500);
  }
  :global([data-theme="dark"]) .why-me .section-title {
    color: var(--dark);
  }
  :global([data-theme="dark"]) .why-me .section-desc {
    color: var(--gray-600);
  }
  @media (prefers-color-scheme: dark) {
    :global(html:not([data-theme="light"])) .why-me {
      background: var(--gray-200) url('/wp2.png') center center / cover no-repeat;
      color: var(--dark);
    }
    :global(html:not([data-theme="light"])) .why-me::before {
      background: linear-gradient(90deg, var(--primary), var(--teal));
    }
    :global(html:not([data-theme="light"])) .why-me::after {
      background: linear-gradient(135deg, rgba(51, 65, 85, 0.92) 0%, rgba(51, 65, 85, 0.85) 100%);
    }
    :global(html:not([data-theme="light"])) .why-me .section-label {
      color: var(--gray-500);
    }
    :global(html:not([data-theme="light"])) .why-me .section-title {
      color: var(--dark);
    }
    :global(html:not([data-theme="light"])) .why-me .section-desc {
      color: var(--gray-600);
    }
  }
  .why-me-container {
    position: relative;
    z-index: 1;
    max-width: 1000px;
    margin: 0 auto;
    padding: 0 24px;
  }
  .section-label--light {
    color: rgba(255, 255, 255, 0.7);
  }
  .section-title--light {
    color: #e2e8f0;
  }
  .section-desc--light {
    color: rgba(255, 255, 255, 0.75);
  }
  .why-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
    max-width: 1000px;
    margin: 0 auto;
  }
  .why-card {
    text-align: left;
    padding: 24px;
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-sm);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }
  .why-number {
    font-family: var(--font-sans);
    font-size: 12px;
    font-weight: 700;
    color: #63b3ed;
    margin-bottom: 12px;
    letter-spacing: 0.05em;
  }
  .why-card h3 {
    font-family: var(--font-display);
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 10px;
    color: #e2e8f0;
  }
  .why-card p {
    font-size: 13px;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.7;
  }
  :global([data-theme="dark"]) .why-card {
    background: var(--gray-100);
    border-color: var(--gray-300);
  }
  :global([data-theme="dark"]) .why-number {
    color: var(--primary);
  }
  :global([data-theme="dark"]) .why-card h3 {
    color: var(--dark);
  }
  :global([data-theme="dark"]) .why-card p {
    color: var(--gray-600);
  }
  @media (prefers-color-scheme: dark) {
    :global(html:not([data-theme="light"])) .why-card {
      background: var(--gray-100);
      border-color: var(--gray-300);
    }
    :global(html:not([data-theme="light"])) .why-number {
      color: var(--primary);
    }
    :global(html:not([data-theme="light"])) .why-card h3 {
      color: var(--dark);
    }
    :global(html:not([data-theme="light"])) .why-card p {
      color: var(--gray-600);
    }
  }

  /* ===== BLOG ===== */
  .blog {
    background: var(--gray-100);
  }
  .blog-placeholder {
    max-width: 480px;
    margin: 0 auto;
    text-align: center;
    padding: 40px 32px;
    background: var(--gray-50);
    border: 1px solid var(--gray-200);
    border-radius: var(--radius-sm);
  }
  .blog-status {
    font-size: 11px;
    font-weight: 600;
    color: var(--gray-400);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 8px;
  }
  .blog-placeholder h3 {
    font-family: var(--font-display);
    font-size: 18px;
    font-weight: 700;
    margin-bottom: 8px;
    color: var(--gray-700);
  }
  .blog-placeholder p {
    font-size: 14px;
    color: var(--gray-500);
    margin-bottom: 20px;
    line-height: 1.6;
  }
  .external-links {
    display: flex;
    justify-content: center;
    gap: 16px;
  }
  .external-link {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 14px;
    font-weight: 500;
    color: var(--primary);
    padding: 10px 20px;
    background: var(--gray-200);
    border-radius: var(--radius-md);
    transition: all 0.2s;
  }
  .external-link:hover {
    background: var(--primary);
    color: #f5f7fa;
  }
  :global([data-theme="dark"]) .external-link:hover {
    color: #12161e;
  }
  @media (prefers-color-scheme: dark) {
    :global(html:not([data-theme="light"])) .external-link:hover {
      color: #12161e;
    }
  }

  /* ===== CTA ===== */
  .cta {
    background: var(--gray-100);
  }
  .cta-inner {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
    padding: 40px 32px;
    background: var(--gray-50);
    border-radius: var(--radius-sm);
    border: 1px solid var(--gray-200);
  }
  .cta-title {
    font-family: var(--font-display);
    font-size: 24px;
    font-weight: 700;
    margin-bottom: 12px;
    color: var(--dark);
  }
  .cta-desc {
    font-size: 15px;
    color: var(--gray-600);
    margin-bottom: 24px;
    line-height: 1.8;
  }
  .cta-note {
    margin-top: 12px;
    font-size: 12px;
    color: var(--gray-500);
  }

  /* ===== RESPONSIVE ===== */
  @media (max-width: 1024px) {
    .hero-inner {
      grid-template-columns: 1fr;
      gap: 40px;
    }
    .hero-content {
      max-width: 100%;
      text-align: center;
      order: 1;
    }
    .profile-card {
      order: 2;
    }
    .hero-cta {
      justify-content: center;
    }
    .profile-card {
      max-width: 400px;
      margin: 0 auto;
    }
    .hero--bg-full::before {
      background: radial-gradient(ellipse at center, rgba(248, 250, 252, 0.9) 0%, rgba(248, 250, 252, 0.6) 100%);
    }
    .problem-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .service-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .why-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    .why-grid > *:last-child {
      grid-column: span 2;
    }
  }

  @media (max-width: 768px) {
    .hero {
      padding: 96px 24px 60px;
    }
    .hero-title {
      font-size: 28px;
    }
    .hero-desc {
      font-size: 15px;
    }
    .profile-stats {
      grid-template-columns: repeat(3, 1fr);
    }
    .section {
      padding: 60px 24px;
    }
    .section-title {
      font-size: 26px;
    }
    .problem-grid {
      grid-template-columns: 1fr;
    }
    .service-grid {
      grid-template-columns: 1fr;
    }
    .why-grid {
      grid-template-columns: 1fr;
    }
    .why-grid > *:last-child {
      grid-column: auto;
    }
    .quick-cards {
      gap: 0.75rem;
    }
    .quick-card {
      min-width: 140px;
      padding: 1rem 1.25rem;
    }
    .quick-icon {
      font-size: 1.5rem;
    }
    .quick-label {
      font-size: 0.9rem;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding: 88px 16px 48px;
    }
    .hero-title {
      font-size: 26px;
    }
    .hero-badge {
      font-size: 12px;
    }
    .hero-cta {
      flex-direction: column;
      width: 100%;
    }
    .btn-primary, .btn-secondary {
      width: 100%;
      justify-content: center;
    }
    .profile-card {
      padding: 24px 20px;
    }
    .profile-stats {
      gap: 8px;
    }
    .stat-value {
      font-size: 20px;
    }
    .section {
      padding: 48px 16px;
    }
    .section-title {
      font-size: 22px;
    }
    .problem-card {
      padding: 20px;
    }
    .service-card {
      padding: 24px 20px;
    }
    .cta-title {
      font-size: 22px;
    }
    .external-links {
      flex-direction: column;
    }
    .quick-access {
      padding: 1.5rem 1rem;
    }
    .quick-cards {
      flex-direction: column;
      align-items: stretch;
      gap: 0.625rem;
    }
    .quick-card {
      flex-direction: row;
      justify-content: flex-start;
      gap: 1rem;
      padding: 1rem 1.25rem;
      min-width: auto;
    }
    .quick-card > * {
      text-align: left;
    }
  }

  @media (hover: none) {
    .quick-card:hover,
    .problem-card:hover,
    .service-card:hover,
    .why-card:hover {
      transform: none;
    }
    .quick-card:active {
      transform: scale(0.98);
    }
  }
</style>
