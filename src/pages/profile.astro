---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';
import profileImage from '../assets/images/profile.jpg';
import { ArrowRight } from '@lucide/astro';
import yaml from 'js-yaml';
import fs from 'node:fs';
import path from 'node:path';

// YAMLファイルを読み込み
const yamlPath = path.join(process.cwd(), 'src/data/profile.yaml');
const profileData = yaml.load(fs.readFileSync(yamlPath, 'utf8')) as {
  name: string;
  subtitle: string;
  badges: { label: string }[];
  stats: { number: string; label: string }[];
  career: { period: string; title: string; description: string; tags: string[] }[];
  qualifications: { icon: string; name: string; description: string }[];
  story: {
    title: string;
    subtitle: string;
    grandfatherWords: string;
    content: string;
  };
  mottos: { text: string; meaning: string }[];
  philosophy: { title: string; content: string }[];
  cta: { title: string; description: string; buttonText: string; buttonLink: string };
};
---

<Layout
  title="プロフィール | ししゃもカンパニー"
  description="吉川昌宏のプロフィール。中小企業診断士・ITストラテジスト。Web開発12年、会計事務所1年の経験を活かし、IT × 経営 × 財務で中小企業の挑戦を後押しします。"
  currentPage="profile"
>
  <!-- Profile Hero -->
  <section class="profile-hero">
    <div class="profile-hero-inner">
      <div class="profile-photo-wrapper">
        <Image src={profileImage} alt="吉川昌宏" width={400} height={400} loading="eager" />
      </div>
      <div class="profile-hero-text">
        <h1>{profileData.name}</h1>
        <p class="subtitle">{profileData.subtitle}</p>
        <div class="profile-badges">
          {profileData.badges.map((badge, index) => (
            <span class={`profile-badge ${index < 2 ? 'highlight' : ''}`}>{badge.label}</span>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Stats -->
  <section class="stats-section">
    <div class="stats-grid">
      {profileData.stats.slice(0, 3).map((stat) => (
        <div class="stat-card">
          <div class="stat-number">
            {stat.number}<span class="stat-unit">{stat.label.includes('年') ? '年' : stat.label.includes('+') ? '' : ''}</span>
          </div>
          <div class="stat-label">{stat.label.replace('年の', '')}</div>
        </div>
      ))}
    </div>
  </section>

  <!-- About My Work -->
  <section class="section">
    <div class="section-inner">
      <h2 class="section-title">私の仕事</h2>
      <div class="about-work">
        <p class="about-lead">
          「数字が見えない」「業務が回らない」——そんな悩みを抱える小規模事業者さんの力になりたい。<br>
          そんな想いで、経営の伴走支援をしています。
        </p>
        <p class="about-desc">
          データサイエンティストとして3年、会計事務所で1年。<br>
          「データで見える化する力」と「会計の実務感覚」、両方の経験を活かして<br>
          記帳・月次の整備から、業務フロー改善、ツール導入まで一気通貫でお手伝いします。
        </p>

        <h3 class="about-subtitle">大切にしている3つのこと</h3>
        <div class="pillars-grid">
          <div class="pillar-card">
            <div class="pillar-number">01</div>
            <h4>数字の土台づくり</h4>
            <p>「今どうなってるの？」がわかる状態を一緒に作ります。記帳・月次を整え、経営判断に使える数字に。</p>
          </div>
          <div class="pillar-card">
            <div class="pillar-number">02</div>
            <h4>属人化の解消</h4>
            <p>「あの人しかわからない」をなくします。業務をフロー・ルール・役割に落とし込んで、誰でも回せる形に。</p>
          </div>
          <div class="pillar-card">
            <div class="pillar-number">03</div>
            <h4>省力化と定着</h4>
            <p>ツールは入れて終わりじゃない。「ちゃんと使える・続けられる」まで一緒に走ります。</p>
          </div>
        </div>

        <div class="about-note">
          <p>DX支援やIT戦略もお受けしますが、派手な提案はしません。<br>
          「何のために？」「今の人員・予算でできる？」を一緒に考えて、<br>
          <strong>本当に回る形</strong>に落とし込むことを大事にしています。</p>
        </div>

        <div class="about-stance">
          <p>お気持ちやご事情はもちろん大切にします。でも、結論は正直にお伝えします。<br>
          「なんとなく良さそう」ではなく、根拠のある選択肢と具体的な手順でサポートします。<br>
          <strong>誠実に、対等に、長く効く改善を一緒に作っていく。それが私のスタイルです。</strong></p>
        </div>
      </div>
    </div>
  </section>

  <!-- Career Timeline -->
  <section class="section section--alt">
    <div class="section-inner">
      <h2 class="section-title">経歴</h2>
      <div class="timeline">
        {profileData.career.map((item) => (
          <div class="timeline-item">
            <div class="timeline-date">{item.period.replace('〜', ' - ')}</div>
            <div class="timeline-title">{item.title}</div>
            <div class="timeline-desc">{item.description}</div>
            <div class="timeline-tags">
              {item.tags.map((tag) => (
                <span class="timeline-tag">{tag}</span>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Qualifications -->
  <section class="section section--alt">
    <div class="section-inner">
      <h2 class="section-title">保有資格</h2>
      <div class="qual-grid">
        {profileData.qualifications.map((qual) => (
          <div class="qual-card">
            <div class="qual-icon">
              {qual.name === '中小企業診断士' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 16v-4M12 8h.01"/></svg>
              )}
              {qual.name === 'ITストラテジスト' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8M12 17v4"/></svg>
              )}
              {qual.name === 'E資格' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2a4 4 0 0 0-4 4v2H6a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V10a2 2 0 0 0-2-2h-2V6a4 4 0 0 0-4-4z"/><circle cx="12" cy="14" r="2"/></svg>
              )}
              {qual.name === 'データベーススペシャリスト' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><ellipse cx="12" cy="5" rx="9" ry="3"/><path d="M3 5v14c0 1.66 4 3 9 3s9-1.34 9-3V5"/><path d="M3 12c0 1.66 4 3 9 3s9-1.34 9-3"/></svg>
              )}
              {qual.name === '応用情報技術者' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="4" width="16" height="16" rx="2"/><path d="M9 9h6M9 13h6M9 17h4"/></svg>
              )}
              {qual.name === '税理士科目合格（簿記論）' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20"/></svg>
              )}
              {qual.name === 'TOEIC815' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M2 12h20M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
              )}
            </div>
            <div class="qual-info">
              <h4>{qual.name}</h4>
              <p>{qual.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Story -->
  <section class="section">
    <div class="section-inner">
      <h2 class="section-title">{profileData.story.title}</h2>
      <p class="section-subtitle">{profileData.story.subtitle}</p>

      <div class="story-content">
        <blockquote class="story-quote">「{profileData.story.grandfatherWords}」</blockquote>
        <p>祖父からいつも言われていた言葉です。</p>

        <p>祖父は、小さな印刷会社を営んでいました。地域の商店や企業のチラシ、名刺、パンフレット。一つひとつ丁寧に仕事をし、お客様に喜ばれていました。「お客さんの商売がうまくいくように」——祖父はいつもそう言って、夜遅くまで働いていました。</p>

        <p>しかし、時代は変わりました。紙からデジタルへ。印刷物の需要は急速に減り、祖父の会社は廃業を余儀なくされました。地域に根ざして何十年も続けてきた会社が、時代の波に飲み込まれていく。その光景を、私はただ見ていることしかできませんでした。</p>

        <p>当時の私には、何もできませんでした。ITの知識も、経営の知識も、財務の知識もない。「何かできることはないか」と思っても、具体的に何をすればいいのかわからない。</p>

        <p class="story-highlight">ただ見ているしかできなかった、あの悔しさ。それが、今の私の原動力です。</p>

        <p>IT、経営、財務。必要なことは何でも学ぼうと決めました。Web開発を12年。データサイエンスを3年。会計事務所で実務を1年。中小企業診断士、ITストラテジスト、データベーススペシャリスト...。「次は絶対に助けられる自分になる」——その一心で、ここまで来ました。</p>

        <blockquote class="story-quote story-quote--resolve">「次は絶対に助ける」</blockquote>

        <p>自分自身、そして自分が関わる大切な人たちを取り巻くこの環境を、少しでも良くしたい。人の役に立つ人間でありたい。その想いが、私を突き動かしています。</p>

        <p>同じように時代の変化に直面している中小企業の社長さんがいたら、今度こそ、一緒に乗り越える力になりたい。ITも、経営も、財務も。必要なことは何でもやる。</p>

        <p class="story-final"><strong>それが、祖父への約束であり、私がこの仕事をする理由です。</strong></p>
      </div>
    </div>
  </section>

  <!-- Philosophy -->
  <section class="section section--alt">
    <div class="section-inner">
      <h2 class="section-title">大切にしていること</h2>
      <div class="philosophy-grid">
        {profileData.philosophy.map((item) => (
          <div class="philosophy-card">
            <h4>{item.title}</h4>
            <p>{item.content}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Mottos -->
  <section class="section">
    <div class="section-inner">
      <h2 class="section-title">座右の銘</h2>
      <div class="mottos-list">
        {profileData.mottos.map((motto) => (
          <div class="motto-card">
            <blockquote class="motto-text">{motto.text}</blockquote>
            <p class="motto-meaning">{motto.meaning}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="section cta">
    <div class="cta-inner">
      <h2 class="cta-title">{profileData.cta.title}</h2>
      <p class="cta-desc" set:html={profileData.cta.description.replace(/\n/g, '<br>')} />
      <a href={profileData.cta.buttonLink} class="btn-primary" data-track-cta="profile">
        {profileData.cta.buttonText}
        <ArrowRight size={14} />
      </a>
      <p class="cta-note">※ 相談だけでも問題ありません。押し売りは一切いたしません。</p>
    </div>
  </section>

</Layout>

<style>
  /* Profile Hero */
  .profile-hero {
    padding: 120px 24px 80px;
    background: var(--color-bg-secondary) url('/wp2.webp') center center / cover no-repeat;
    position: relative;
    overflow: hidden;
  }
  .profile-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: var(--color-overlay-gradient);
  }
  .profile-hero-inner {
    max-width: 900px;
    margin: 0 auto;
    display: flex;
    align-items: center;
    gap: 48px;
    position: relative;
    z-index: 1;
  }
  .profile-photo-wrapper {
    width: 200px;
    height: 200px;
    border-radius: var(--radius-lg);
    overflow: hidden;
    flex-shrink: 0;
    border: 3px solid var(--color-accent);
    box-shadow: var(--shadow-semantic-lg);
  }
  .profile-photo-wrapper img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .profile-hero-text h1 {
    font-family: var(--font-display);
    font-size: 32px;
    font-weight: 700;
    margin-bottom: 8px;
    color: var(--color-text-heading);
  }
  .profile-hero-text .subtitle {
    font-size: 15px;
    color: var(--color-text-secondary);
    margin-bottom: 20px;
    line-height: 1.7;
  }
  .profile-badges {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }
  .profile-badge {
    padding: 6px 14px;
    background: var(--color-bg-tertiary);
    color: var(--color-text);
    border-radius: 100px;
    font-size: var(--font-size-xs);
    font-weight: 500;
  }
  .profile-badge.highlight {
    background: var(--color-accent-subtle);
    color: var(--color-accent-text);
    font-weight: 600;
  }

  /* Stats Section */
  .stats-section {
    padding: 0 24px;
    margin-top: -40px;
    position: relative;
    z-index: 10;
  }
  .stats-grid {
    max-width: 900px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }
  .stat-card {
    background: var(--color-card-bg);
    border: 2px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: 24px;
    text-align: center;
    transition: all 0.2s;
    box-shadow: var(--shadow-semantic-sm);
  }
  .stat-card:hover {
    border-color: var(--color-accent);
  }
  .stat-number {
    font-family: var(--font-sans);
    font-size: var(--font-size-4xl);
    font-weight: 700;
    color: var(--color-accent);
  }
  .stat-unit {
    font-size: var(--font-size-base);
    font-weight: 500;
  }
  .stat-label {
    font-size: 13px;
    color: var(--color-text-secondary);
    margin-top: 4px;
  }

  /* Section Common */
  .section {
    padding: 80px 24px;
  }
  .section--alt {
    background: var(--color-bg-secondary);
  }
  .section-inner {
    max-width: 900px;
    margin: 0 auto;
  }
  .section-title {
    font-size: 22px;
    color: var(--color-text-heading);
    -webkit-text-fill-color: var(--color-text-heading);
    background: none;
    margin-bottom: 32px;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .section-title::before {
    content: '';
    width: 4px;
    height: 24px;
    background: var(--color-accent);
    border-radius: 2px;
  }
  .section-subtitle {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    margin-top: -24px;
    margin-bottom: 32px;
  }

  /* About My Work */
  .about-work {
    background: var(--color-card-bg);
    border: 2px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: 40px;
    box-shadow: var(--shadow-semantic-sm);
  }
  .about-lead {
    font-size: 17px;
    line-height: 1.9;
    color: var(--color-text-heading);
    margin-bottom: 16px;
    font-weight: 500;
  }
  .about-desc {
    font-size: 15px;
    line-height: 2;
    color: var(--color-text);
    margin-bottom: 32px;
  }
  .about-subtitle {
    font-family: var(--font-display);
    font-size: var(--font-size-lg);
    font-weight: 700;
    color: var(--color-text-heading);
    margin-bottom: 20px;
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .about-subtitle::before {
    content: '';
    width: 3px;
    height: 18px;
    background: var(--color-accent);
    border-radius: 2px;
  }
  .pillars-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
    margin-bottom: 32px;
  }
  .pillar-card {
    background: var(--color-bg-tertiary);
    border-radius: var(--radius-sm);
    padding: 24px 20px;
    position: relative;
  }
  .pillar-number {
    font-family: var(--font-sans);
    font-size: var(--font-size-xs);
    font-weight: 700;
    color: var(--color-accent);
    margin-bottom: 8px;
    letter-spacing: 0.05em;
  }
  .pillar-card h4 {
    font-size: 15px;
    font-weight: 700;
    color: var(--color-text-heading);
    margin-bottom: 8px;
  }
  .pillar-card p {
    font-size: 13px;
    line-height: 1.7;
    color: var(--color-text-secondary);
  }
  .about-note {
    background: var(--color-accent-subtle);
    border-radius: var(--radius-sm);
    padding: 20px 24px;
    margin-bottom: 24px;
  }
  .about-note p {
    font-size: var(--font-size-sm);
    line-height: 1.9;
    color: var(--color-text);
  }
  .about-stance {
    padding-top: 8px;
  }
  .about-stance p {
    font-size: 15px;
    line-height: 2;
    color: var(--color-text);
  }
  .about-stance strong {
    color: var(--color-accent);
  }

  /* Timeline */
  .timeline {
    position: relative;
    padding-left: 32px;
  }
  .timeline::before {
    content: '';
    position: absolute;
    left: 0;
    top: 8px;
    bottom: 8px;
    width: 2px;
    background: var(--color-border);
    border-radius: 2px;
  }
  .timeline-item {
    position: relative;
    padding-bottom: 32px;
  }
  .timeline-item:last-child {
    padding-bottom: 0;
  }
  .timeline-item::before {
    content: '';
    position: absolute;
    left: -32px;
    top: 6px;
    width: 12px;
    height: 12px;
    background: var(--color-accent);
    border-radius: 50%;
    transform: translateX(-50%);
    border: 2px solid var(--color-bg);
  }
  .timeline-date {
    font-size: var(--font-size-xs);
    color: var(--color-accent);
    font-weight: 600;
    margin-bottom: 4px;
    letter-spacing: 0.02em;
  }
  .timeline-title {
    font-size: var(--font-size-base);
    font-weight: 700;
    margin-bottom: 8px;
    color: var(--color-text-heading);
  }
  .timeline-desc {
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
    line-height: 1.8;
  }
  .timeline-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 12px;
  }
  .timeline-tag {
    font-size: 11px;
    padding: 4px 10px;
    background: var(--color-bg-tertiary);
    border-radius: 100px;
    color: var(--color-text);
  }

  /* Qualifications */
  .qual-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
  }
  .qual-card {
    background: var(--color-card-bg);
    border: 2px solid var(--color-border);
    border-radius: var(--radius-sm);
    padding: 16px 20px;
    display: flex;
    align-items: center;
    gap: 16px;
    transition: all 0.2s;
    box-shadow: var(--shadow-semantic-sm);
  }
  .qual-card:hover {
    border-color: var(--color-accent);
  }
  .qual-icon {
    width: 44px;
    height: 44px;
    background: var(--color-accent-subtle);
    border-radius: var(--radius-sm);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }
  .qual-icon svg {
    width: 22px;
    height: 22px;
    color: var(--color-accent);
  }
  .qual-info h4 {
    font-size: var(--font-size-sm);
    font-weight: 600;
    color: var(--color-text-heading);
    margin-bottom: 2px;
  }
  .qual-info p {
    font-size: var(--font-size-xs);
    color: var(--color-text-secondary);
  }

  /* Story */
  .story-content {
    background: var(--color-card-bg);
    border: 2px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: 40px;
    box-shadow: var(--shadow-semantic-sm);
  }
  .story-content p {
    font-size: 15px;
    line-height: 2;
    color: var(--color-text);
    margin-bottom: 20px;
  }
  .story-content p:last-child {
    margin-bottom: 0;
  }
  .story-highlight {
    color: var(--color-text-heading) !important;
    font-weight: 600;
    font-size: var(--font-size-base);
  }
  .story-quote {
    font-family: var(--font-display);
    font-size: 20px;
    font-weight: 700;
    color: var(--color-accent);
    text-align: center;
    padding: 24px 0;
    margin: 24px 0;
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
  }
  .story-quote:first-child {
    border-top: none;
    margin-top: 0;
    padding-top: 0;
  }
  .story-quote--resolve {
    font-size: 22px;
    color: var(--color-accent);
    background: var(--color-accent-subtle);
    border: none;
    border-radius: var(--radius-sm);
    padding: 20px;
  }
  .story-final {
    text-align: center;
    font-size: var(--font-size-base);
    margin-top: 8px;
  }
  .story-final strong {
    color: var(--color-text-heading);
  }

  /* Philosophy */
  .philosophy-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 16px;
  }
  .philosophy-card {
    background: var(--color-card-bg);
    border: 2px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: 24px;
    transition: all 0.2s;
    box-shadow: var(--shadow-semantic-sm);
  }
  .philosophy-card:hover {
    border-color: var(--color-accent);
  }
  .philosophy-card h4 {
    font-family: var(--font-display);
    font-size: var(--font-size-base);
    font-weight: 700;
    color: var(--color-text-heading);
    margin-bottom: 12px;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .philosophy-card h4::before {
    content: '';
    width: 4px;
    height: 16px;
    background: var(--color-accent);
    border-radius: 2px;
  }
  .philosophy-card p {
    font-size: var(--font-size-sm);
    line-height: 1.8;
    color: var(--color-text-secondary);
  }

  /* Mottos */
  .mottos-list {
    display: flex;
    flex-direction: column;
    gap: 20px;
  }
  .motto-card {
    background: var(--color-card-bg);
    border: 2px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: 32px;
    border-left: 4px solid var(--color-accent);
    box-shadow: var(--shadow-semantic-sm);
  }
  .motto-text {
    font-family: var(--font-display);
    font-size: var(--font-size-lg);
    font-weight: 600;
    color: var(--color-text-heading);
    line-height: 1.7;
    margin-bottom: 16px;
    text-align: center;
  }
  .motto-text::before {
    content: '\201C';
    color: var(--color-accent);
    font-size: 48px;
    font-family: Georgia, serif;
    line-height: 1;
    margin-right: 4px;
    opacity: 0.5;
  }
  .motto-text::after {
    content: '\201D';
    color: var(--color-accent);
    font-size: 48px;
    font-family: Georgia, serif;
    line-height: 1;
    margin-left: 4px;
    opacity: 0.5;
  }
  .motto-meaning {
    font-size: var(--font-size-sm);
    line-height: 1.8;
    color: var(--color-text-secondary);
    padding-left: 0;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .profile-hero {
      padding: 96px 24px 60px;
    }
    .profile-hero-inner {
      flex-direction: column;
      text-align: center;
      gap: 24px;
    }
    .profile-photo-wrapper {
      width: 160px;
      height: 160px;
    }
    .profile-hero-text h1 {
      font-size: 26px;
    }
    .profile-badges {
      justify-content: center;
    }
    .stats-section {
      margin-top: -30px;
    }
    .stats-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 12px;
    }
    .stat-card {
      padding: 20px 16px;
    }
    .stat-number {
      font-size: 24px;
    }
    .section {
      padding: 48px 20px;
    }
    .section-title {
      font-size: 19px;
    }
    .qual-grid {
      grid-template-columns: 1fr;
    }
    .story-content {
      padding: 24px 20px;
    }
    .story-quote {
      font-size: var(--font-size-lg);
    }
    .story-quote--resolve {
      font-size: 20px;
    }
    .philosophy-grid {
      grid-template-columns: 1fr;
    }
    .motto-card {
      padding: 20px 16px;
    }
    .motto-text {
      font-size: var(--font-size-sm);
    }
    .about-work {
      padding: 24px 16px;
    }
    .pillars-grid {
      grid-template-columns: 1fr;
      gap: 12px;
    }
  }

  @media (max-width: 480px) {
    .profile-hero {
      padding: 88px 16px 48px;
    }
    .profile-photo-wrapper {
      width: 140px;
      height: 140px;
    }
    .profile-hero-text h1 {
      font-size: 22px;
    }
    .profile-badge {
      font-size: 11px;
      padding: 5px 10px;
    }
    .stats-grid {
      gap: 8px;
    }
    .stat-card {
      padding: 16px 12px;
    }
    .stat-number {
      font-size: 20px;
    }
    .stat-label {
      font-size: 11px;
    }
    .section {
      padding: 40px 16px;
    }
    .timeline {
      padding-left: 24px;
    }
    .timeline-item::before {
      left: -24px;
      width: 10px;
      height: 10px;
    }
  }

  @media (hover: none) {
    .stat-card:hover,
    .qual-card:hover,
    .philosophy-card:hover {
      border-color: var(--color-border);
    }
  }
</style>
